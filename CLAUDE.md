# IBIS ETL — Claude Code Instructions

## Git Worktrees

Worktree directory: `.worktrees/` (project-local, gitignored)

Create feature branches with:
```bash
git worktree add .worktrees/<branch-name> -b <branch-name>
```
